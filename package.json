{
  "name": "petit-pas",
  "version": "2.0.0",
  "description": "Language learning trainer with spaced repetition, gamification, and daily sessions",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "npx serve . -l 3000",
    "test": "node tests/store.test.mjs && node tests/render.test.mjs",
    "lint": "npx eslint app/ tests/",
    "format": "npx prettier --write 'app/**/*.{js,css}' 'tests/**/*.mjs'"
  },
  "keywords": [
    "language-learning",
    "spaced-repetition",
    "srs",
    "flashcards",
    "gamification"
  ],
  "license": "MIT",
  "devDependencies": {
    "jsdom": "^29.1.1"
  }
}
